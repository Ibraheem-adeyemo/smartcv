import {render} from '@testing-library/react' 
import { Container } from '.';

describe('Container', () => {
    test('Container render correctly', () => {
        // render(<Container>

        // </Container>)
    })
})